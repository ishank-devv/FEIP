// Q2- What is Function Expression

const square = function (num) {
  return num * num;
};

//Q- What is Anonymous function
// right part of = is Anonymous function( function with no Name ) cab be
// assigned to the variable or can be passed as callback
console.log(square(5));
